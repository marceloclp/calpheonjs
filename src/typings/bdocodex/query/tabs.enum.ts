import { Types } from './types.enum'

/**
 * Maps the query tabs ids to their respective query types.
 */
export enum Tabs {
    questreward = Types.QuestReward,
    recipematerial = Types.RecipeMaterial,
}